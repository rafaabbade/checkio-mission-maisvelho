def irmao_mais_velho(a,b,c):
    # escreva aqui seu código
    return None


print("Example:")
print(irmao_mais_velho(12,8,7))

# As funções assert são utilizadas para verificar se o resultado da função chamada é o esperado

assert irmao_mais_velho(20, 22, 18) == "O irmão mais velho tem 22 anos" 
assert irmao_mais_velho(30, 25, 35) == "O irmão mais velho tem 35 anos" 
assert irmao_mais_velho(30, 20, 22) == "O irmão mais velho tem 30 anos"
    
print("Seu código passou pelos testes, clique em Check para avançar!")
